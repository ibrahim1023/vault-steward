# Core Boundary

Core modules must not import Obsidian APIs. The plugin and UI integrate with Obsidian through narrow adapters; deterministic scanner, graph, policy, reference, and validation modules remain platform-independent.
